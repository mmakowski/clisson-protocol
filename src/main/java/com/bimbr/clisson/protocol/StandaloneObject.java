package com.bimbr.clisson.protocol;

/**
 * A marker interface for objects that can be transmitted on their own.
 * 
 * @author mmakowski
 * @since 1.0.0
 */
public interface StandaloneObject {
  // marker
}
